move_base_flex/TEBPlanner:
  weight_viapoint: 200.0  # overweight viapoints to stick closely to the path

  # loose goal reached criteria, ignoring orientation and velocity at goal
  xy_goal_tolerance: 0.2
  yaw_goal_tolerance: 3.2
  trans_stopped_vel: 10.0
  theta_stopped_vel: 10.0
